require('nvim-treesitter.configs').setup {
    ensure_installed = {},

    auto_intall = false,

    highlight = { enable = true},

    indent = { enable = true},
    autotag = {
        enable = true
    }
}
